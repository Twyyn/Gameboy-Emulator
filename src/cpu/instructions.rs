use crate::cpu::opcodes::{Mnemonic, MnemonicCB};

pub struct Instruction {
    opcode: Mnemonic,
}

impl Instruction {
    pub fn decode(&self, opcode: Mnemonic) {
        match opcode {
            
        }
    }
}
